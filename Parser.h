#ifndef PARSER_H
#define PARSER_H

#include <vector>
#include <string>

class Parser {
public:
	// Takes in a user-input string and parses it into separate tokens
	std::vector<std::string> get_parsed_command(std::string);
private:



};



#endif /* PARSER_H */